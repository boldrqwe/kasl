FROM jlesage/firefox

# Устанавливаем VNC_PASSWORD через переменную окружения
ENV VNC_PASSWORD=123

# Открываем необходимые порты
EXPOSE 5800
EXPOSE 5900

# Указываем команду запуска Firefox-контейнера
CMD ["/init"]
